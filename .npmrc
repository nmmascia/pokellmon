# Motion+ private registry. References an env var — safe to commit; the token is never stored here.
# Requires MOTION_AUTH_TOKEN to be set in your shell when running install.
@motionplus:registry=https://api.motion.dev/npm/
//api.motion.dev/npm/:_authToken=${MOTION_AUTH_TOKEN}
